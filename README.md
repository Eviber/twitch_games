A collection of games using the Twitch API

- Hangman

Run with `cargo run`.

### Dependencies

The Rust toolchain.

Install with:
```sh
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```
